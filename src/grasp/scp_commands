1. Send image files from lab computer to hive
scp -r demo/ username@hivehost:~/whatever/

2. Receive image files from hive to laptop
scp -r username@hivehost:~/whatever/ ~/colmap_workspace

3. Send mesh output from laptop to hive
scp ~/path_to_mesh username@hivehost:~/whatever/mesh_output

4. Receive mesh output from hive to lab computer
scp username@hivehost:~/whatever/mesh_output/mesh_file_name demo/

